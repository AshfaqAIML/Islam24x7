"""Book metadata extraction and review.

Combines PDF metadata, filename signals, and the first three pages into an
editable JSON metadata record with per-field confidence, reviewed by a human
before the book is treated as verified.
"""