# exports/

Portable knowledge-base exports produced on request — e.g. JSON/Markdown dumps
of validated content, snapshots for a web/mobile app, or RAG index bundles.

## Rules

- Generated by the export stage from validated data only.
- **Never committed** to git (can contain bulk content and be large).
- Never treated as a source of truth: `data/raw/` + the database are.