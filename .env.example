# Copy to .env and adjust. Never commit real values.
KB_DATA_DIR=./data
KB_LOG_LEVEL=INFO

# PostgreSQL (future milestone: database)
KB_DATABASE_URL=postgresql+psycopg://user:password@localhost:5433/knowledge_base
KB_TEST_DATABASE_URL=postgresql+psycopg://user:password@localhost:5433/knowledge_base_test